# src/db/__init__.py — Initialisation du module base de données

## Rôle

Expose `SQLiteManager` depuis le sous-module `src.db.sqlite_manager`.

---

## Export

```python
from src.db.sqlite_manager import SQLiteManager
```

Permet d'importer directement avec :

```python
from src.db import SQLiteManager
```
